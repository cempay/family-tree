import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  sex: {
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: 8,
  },
  submit: {
    // TODO not work
    marginTop: 40,
  },
});
